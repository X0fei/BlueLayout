<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ProektChernovik" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:fontFamily">@font/nunito</item>
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.BlueLayout" parent="Base.Theme.ProektChernovik" />
    <style name="AuthoEditText" parent="Widget.AppCompat.EditText">
        <item name="android:layout_width">270dp</item>
        <item name="android:layout_height">40dp</item>
        <item name="android:background">@drawable/edittext_background</item>
    </style>

    <style name="AlertDialog.Butt" parent="TextAppearance.AppCompat.Widget.Button">
         <item name="android:clickable">true</item>
        <item name="android:onClick">onClick</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">22dp</item>
        <item name="textAllCaps">false</item>
        <item name="android:layout_width">124dp</item>
        <item name="android:layout_height">48dp</item>
    </style>
</resources>